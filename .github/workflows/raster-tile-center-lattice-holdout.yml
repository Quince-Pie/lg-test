name: Test Apple Metal tile center p27 lattice

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/raster-tile-center-lattice-holdout.yml
      - Analysis/open_raster_tile_center_lattice_holdout.py
      - Analysis/raster_tile_center_lattice_preregistration.json
      - Analysis/raster_tile_selector_model.py
      - Analysis/raster_tile_selector_model_v2.py
      - Analysis/raster_tile_selector_model_v4.py
      - Analysis/raster_tile_selector_model_v5.py
      - Analysis/raster_tile_selector_model_v6.py
      - Analysis/raster_tile_v6_sealed_predictions.zlib
      - Analysis/test_validate_raster_tile_center_lattice_holdout.py
      - Analysis/validate_raster_tile_center_lattice_holdout.py
      - Sources/GlassRasterTileNumerator/**

concurrency:
  group: raster-tile-center-lattice-holdout-${{ github.ref }}
  cancel-in-progress: true

jobs:
  raster-tile-center-lattice-holdout:
    runs-on: macos-26
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Require macOS 26+
        run: |
          sw_vers
          test "$(sw_vers -productVersion | cut -d. -f1)" -ge 26

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Test frozen p27-lattice contract
        run: |
          python3 -m unittest discover -s Analysis \
            -p 'test_validate_raster_tile_center_lattice_holdout.py' -v

      - name: Build p27-lattice probe
        id: build
        continue-on-error: true
        run: |
          mkdir -p raster-tile-center-lattice
          set -o pipefail
          swiftc -D TILE_CENTER_LATTICE_HOLDOUT -O -parse-as-library \
            -target "$(uname -m)-apple-macos26.0" \
            Sources/GlassRasterTileNumerator/main.swift \
            -o glass-raster-tile-center-lattice 2>&1 |
            tee raster-tile-center-lattice/build.log

      - name: Capture unseen p27-lattice records
        id: capture
        if: steps.build.outcome == 'success'
        continue-on-error: true
        env:
          SWIFT_BACKTRACE: enable
        run: |
          ./glass-raster-tile-center-lattice raster-tile-center-lattice

      - name: Validate capture without opening sealed words
        id: validate
        if: steps.capture.outcome == 'success'
        continue-on-error: true
        run: |
          python3 Analysis/validate_raster_tile_center_lattice_holdout.py \
            raster-tile-center-lattice \
            --output raster-tile-center-lattice/validation.json

      - name: Open committed sealed predictions
        id: opening
        if: steps.validate.outcome == 'success'
        continue-on-error: true
        run: |
          python3 Analysis/open_raster_tile_center_lattice_holdout.py \
            raster-tile-center-lattice \
            --output raster-tile-center-lattice/holdout-opening.json

      - name: Upload p27-lattice evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: liquid-glass-raster-tile-center-lattice-${{ github.run_id }}
          path: raster-tile-center-lattice
          if-no-files-found: error
          retention-days: 7

      - name: Enforce p27-lattice evidence
        if: always()
        run: |
          test "${{ steps.build.outcome }}" = success
          test "${{ steps.capture.outcome }}" = success
          test "${{ steps.validate.outcome }}" = success
          test "${{ steps.opening.outcome }}" = success
          test -s raster-tile-center-lattice/manifest.json
          test -s raster-tile-center-lattice/validation.json
          test -s raster-tile-center-lattice/holdout-opening.json
          test -s raster-tile-center-lattice/raster-tile-numerator.raw
