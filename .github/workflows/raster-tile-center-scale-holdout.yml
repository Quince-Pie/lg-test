name: Test Apple Metal tile center scale switch

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/raster-tile-center-scale-holdout.yml
      - Analysis/open_raster_tile_center_scale_holdout.py
      - Analysis/raster_tile_center_scale_preregistration.json
      - Analysis/raster_tile_selector_model.py
      - Analysis/raster_tile_selector_model_v2.py
      - Analysis/raster_tile_selector_model_v4.py
      - Analysis/raster_tile_selector_model_v6.py
      - Analysis/raster_tile_selector_model_v7.py
      - Analysis/raster_tile_v7_sealed_predictions.zlib
      - Analysis/test_validate_raster_tile_center_scale_holdout.py
      - Analysis/validate_raster_tile_center_scale_holdout.py
      - Sources/GlassRasterTileNumerator/**

concurrency:
  group: raster-tile-center-scale-holdout-${{ github.ref }}
  cancel-in-progress: true

jobs:
  raster-tile-center-scale-holdout:
    runs-on: macos-26
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Require macOS 26+
        run: |
          sw_vers
          test "$(sw_vers -productVersion | cut -d. -f1)" -ge 26

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Test frozen scale-switch contract
        run: |
          python3 -m unittest discover -s Analysis \
            -p 'test_validate_raster_tile_center_scale_holdout.py' -v

      - name: Build scale-switch probe
        id: build
        continue-on-error: true
        run: |
          mkdir -p raster-tile-center-scale
          set -o pipefail
          swiftc -D TILE_CENTER_SCALE_HOLDOUT -O -parse-as-library \
            -target "$(uname -m)-apple-macos26.0" \
            Sources/GlassRasterTileNumerator/main.swift \
            -o glass-raster-tile-center-scale 2>&1 |
            tee raster-tile-center-scale/build.log

      - name: Capture unseen scale-switch records
        id: capture
        if: steps.build.outcome == 'success'
        continue-on-error: true
        env:
          SWIFT_BACKTRACE: enable
        run: |
          ./glass-raster-tile-center-scale raster-tile-center-scale

      - name: Validate capture without opening sealed words
        id: validate
        if: steps.capture.outcome == 'success'
        continue-on-error: true
        run: |
          python3 Analysis/validate_raster_tile_center_scale_holdout.py \
            raster-tile-center-scale \
            --output raster-tile-center-scale/validation.json

      - name: Open committed sealed predictions
        id: opening
        if: steps.validate.outcome == 'success'
        continue-on-error: true
        run: |
          python3 Analysis/open_raster_tile_center_scale_holdout.py \
            raster-tile-center-scale \
            --output raster-tile-center-scale/holdout-opening.json

      - name: Upload scale-switch evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: liquid-glass-raster-tile-center-scale-${{ github.run_id }}
          path: raster-tile-center-scale
          if-no-files-found: error
          retention-days: 7

      - name: Enforce scale-switch evidence
        if: always()
        run: |
          test "${{ steps.build.outcome }}" = success
          test "${{ steps.capture.outcome }}" = success
          test "${{ steps.validate.outcome }}" = success
          test "${{ steps.opening.outcome }}" = success
          test -s raster-tile-center-scale/manifest.json
          test -s raster-tile-center-scale/validation.json
          test -s raster-tile-center-scale/holdout-opening.json
          test -s raster-tile-center-scale/raster-tile-numerator.raw
