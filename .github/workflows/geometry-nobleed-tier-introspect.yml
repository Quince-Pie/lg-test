name: Resolve Liquid Glass no-bleed allocation tier

on:
  workflow_dispatch:

concurrency:
  group: geometry-nobleed-tier-introspect-${{ github.ref }}
  cancel-in-progress: true

jobs:
  geometry-nobleed-tier:
    name: ${{ matrix.geometry }}
    runs-on: macos-26
    timeout-minutes: 8
    strategy:
      fail-fast: false
      matrix:
        geometry:
          - circle-047-tier-x-512
          - circle-047-tier-x-513
          - circle-047-tier-x-514
          - circle-047-tier-x-515
          - circle-047-tier-x-516
          - circle-047-tier-x-517
          - circle-047-tier-x-518
          - circle-047-tier-x-519
          - circle-047-tier-x-520
          - circle-047-tier-x-521
          - circle-047-tier-x-522
          - circle-047-tier-x-523
          - circle-047-tier-x-524
          - circle-047-tier-x-525
          - circle-047-tier-x-526
          - circle-047-tier-x-527
          - circle-047-tier-y-512
          - circle-047-tier-y-513
          - circle-047-tier-y-514
          - circle-047-tier-y-515
          - circle-047-tier-y-516
          - circle-047-tier-y-517
          - circle-047-tier-y-518
          - circle-047-tier-y-519
          - circle-047-tier-y-520
          - circle-047-tier-y-521
          - circle-047-tier-y-522
          - circle-047-tier-y-523
          - circle-047-tier-y-524
          - circle-047-tier-y-525
          - circle-047-tier-y-526
          - circle-047-tier-y-527
    env:
      LG_GLASS_MATERIAL: regular
      LG_GLASS_APPEARANCE: light
      LG_GLASS_GEOMETRY: ${{ matrix.geometry }}
      LG_GEOMETRY_POLICY: "1"
    steps:
      - uses: actions/checkout@v4

      - name: Require macOS 26+
        run: |
          sw_vers
          test "$(sw_vers -productVersion | cut -d. -f1)" -ge 26

      - name: Select standard visual accessibility state
        run: |
          defaults write com.apple.universalAccess reduceTransparency -bool false
          defaults write com.apple.universalAccess reduceMotion -bool false
          defaults write com.apple.universalAccess increaseContrast -bool false
          killall cfprefsd 2>/dev/null || true

      - name: Build compact geometry-policy probe
        run: |
          mkdir -p geometry-policy
          xcrun clang \
            -std=c23 \
            -O2 \
            -Wall \
            -Wextra \
            -Werror \
            -target "$(uname -m)-apple-macos26.0" \
            -c Sources/GlassIntrospect/MatrixBridge.c \
            -o geometry-policy/MatrixBridge.o
          swiftc -O -parse-as-library \
            -target "$(uname -m)-apple-macos26.0" \
            -import-objc-header Sources/GlassIntrospect/MatrixBridge.h \
            Sources/GlassIntrospect/HalfBlendProbe.swift \
            Sources/GlassIntrospect/HalfDotProbe.swift \
            Sources/GlassIntrospect/HalfIntrinsicProbe.swift \
            Sources/GlassIntrospect/SDFStageProbe.swift \
            Sources/GlassIntrospect/main.swift \
            geometry-policy/MatrixBridge.o \
            -o glassintrospect

      - name: Capture real no-bleed tier metadata
        run: |
          ./glassintrospect geometry-policy

      - name: Enforce compact no-bleed evidence
        run: |
          python3 - <<'PY'
          import json
          import os
          import pathlib

          root = pathlib.Path("geometry-policy")
          report = json.loads(
              (root / "geometry-policy.json").read_text(
                  encoding="utf-8"
              )
          )
          if (
              report.get("schemaVersion") != 1
              or report.get("probe") != "compact-geometry-policy"
              or report.get("executed") is not True
          ):
              raise SystemExit(
                  f"geometry-policy report differs: {report}"
              )
          geometry = report.get("geometry", {})
          if (
              geometry.get("name") != os.environ["LG_GLASS_GEOMETRY"]
              or geometry.get("width") != 47
              or geometry.get("height") != 47
          ):
              raise SystemExit(
                  f"geometry identity differs: {geometry}"
              )
          profile = report.get("materialProfile", {})
          if (
              profile.get("material") != "regular"
              or profile.get("requestedAppearance") != "light"
              or profile.get("effectiveAppearanceMatchesRequest")
                  is not True
          ):
              raise SystemExit(
                  f"geometry material profile differs: {profile}"
              )
          render = report.get("carendererEvidence", {})
          buffers = render.get("metalBufferSnapshots", {}).get(
              "snapshots", []
          )
          textures = render.get("metalTextureSnapshots", {}).get(
              "snapshots", []
          )

          def fragment(record):
              return (
                  record.get("pipeline", {})
                  .get("creationDescriptor", {})
                  .get("fragmentFunction", "")
              )

          vertices = [
              record
              for record in buffers
              if record.get("stage") == "vertex"
              and record.get("index") == 1
              and fragment(record)
                  == "glass_background_sdf_no_bleed_lph"
              and isinstance(
                  record.get("payload", {}).get("hex"), str
              )
          ]
          sources = [
              record
              for record in textures
              if record.get("index") == 3
              and fragment(record)
                  == "glass_background_sdf_no_bleed_lph"
          ]
          if len(vertices) != 2 or len(sources) != 1:
              raise SystemExit(
                  "no-bleed buffer/texture cardinality differs: "
                  f"vertices={len(vertices)}, sources={len(sources)}"
              )
          source = sources[0]
          if (
              source.get("width") != 64
              or source.get("height") != 64
              or source.get("mipmapLevelCount") != 2
              or source.get("rawCapture") is not False
              or source.get("reason")
                  != "metadata-only geometry-policy capture"
          ):
              raise SystemExit(
                  f"no-bleed source texture differs: {source}"
              )
          if list(root.glob("*.raw")):
              raise SystemExit(
                  "compact geometry capture emitted raw stage dumps"
              )
          PY

      - name: Upload compact no-bleed tier evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: liquid-glass-geometry-nobleed-tier-${{ matrix.geometry }}-${{ github.run_id }}
          path: geometry-policy
          if-no-files-found: error
          retention-days: 7
